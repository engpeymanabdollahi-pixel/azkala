<?php

/**
 * تنظیمات امنیتی سطح زیرساخت (نه site_settings دیتابیسی — این‌ها قبل از
 * اینکه دیتابیس/کاربری وجود داشته باشد لازم‌اند).
 */
return [
    /**
     * شماره تلفن کاربری که با دستور `php artisan app:bootstrap-super-admin`
     * به‌عنوان اولین Super Admin تعیین می‌شود. باید در .env سرور تنظیم شود؛
     * بدون آن، دستور بوت‌استرپ اجرا نمی‌شود (رجوع به
     * BootstrapSuperAdminCommand).
     *
     * مقداری اینجا fabricate نشده — عمداً env(...) بدون default واقعی
     * است تا در محیط‌هایی که این متغیر تنظیم نشده، صراحتاً خالی/null
     * بماند، نه یک شماره‌ی نمونه‌ی گمراه‌کننده.
     */
    'super_admin_phone' => env('AZKALA_SUPER_ADMIN_PHONE'),
];
